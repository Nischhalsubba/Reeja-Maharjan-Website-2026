---
type Props = {
  id?: string;
  eyebrow?: string;
  title: string;
  summary: string;
};

const { id, eyebrow, title, summary } = Astro.props;
---

<div class="section-intro" id={id}>
  {eyebrow && <p class="section-intro__eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  <p>{summary}</p>
</div>
