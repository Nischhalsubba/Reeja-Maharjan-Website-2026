---
import BaseLayout from '../layouts/BaseLayout.astro';
import { blogPosts } from '../content/blog';
import { siteCopy } from '../content/site-copy';

const copy = siteCopy.maternalHealth;
const posts = blogPosts.filter((post) =>
  [post.primaryKeyword, ...post.supportingKeywords, ...post.tags]
    .join(' ')
    .toLowerCase()
    .match(/maternal|newborn|postnatal|breastfeeding|pregnan|mother/)
).slice(0, 3);
---

<BaseLayout title={copy.title} description={copy.description}>
  <div class="site-page">
    <section class="site-page__hero">
      <div class="container site-page__hero-grid">
        <div>
          <p class="site-eyebrow">Maternal health</p>
          <h1>Maternal health in Reeja Maharjan’s nursing and research work.</h1>
          <p class="site-page__lede">Maternal-newborn nursing experience combined with current research, monitoring support, follow-up, and patient education.</p>
        </div>
        <dl class="site-page__summary">
          {copy.summary.map(([label, value]) => <div><dt>{label}</dt><dd>{value}</dd></div>)}
        </dl>
      </div>
    </section>

    <section class="site-section">
      <div class="container">
        <div class="site-section__head">
          <div><p class="site-eyebrow">Selected writing</p><h2>Maternal-health and newborn-care articles.</h2></div>
          <p>Three practical, source-linked reads from Reeja.</p>
        </div>
        <div class="site-grid">
          {posts.map((post) => (
            <article class="site-card">
              <p class="site-eyebrow">By Reeja Maharjan · {post.readMinutes} min read</p>
              <h3>{post.title}</h3>
              <p>{post.excerpt}</p>
              <p><a href={`/blog/${post.slug}/`}>Read article</a></p>
            </article>
          ))}
        </div>
        <p><a href="/blog/">View all writing by Reeja</a></p>
      </div>
    </section>

    <section class="site-section site-section--dark">
      <div class="container site-page__split">
        <div><p class="site-eyebrow">Professional work</p><h2>Explore Reeja’s maternal-health research.</h2><p>Review her current research role, clinical background, and credentials.</p></div>
        <div class="site-actions"><a class="site-button" href="/clinical-research/">Research experience</a><a class="site-button" href="/contact/">Contact Reeja</a></div>
      </div>
    </section>
  </div>
</BaseLayout>
