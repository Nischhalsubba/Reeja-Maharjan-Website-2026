---
import { professionalFacts } from '../content/professional-facts';

type Props = { name: string };
const { name } = Astro.props;
const path = Astro.url.pathname;
const current = (href: string) => href === '/' ? path === '/' : path.startsWith(href);
const links = [
  { href: '/#experience', label: 'Experience' },
  { href: '/maternal-health/', label: 'Maternal health' },
  { href: '/nursing-practice/', label: 'Nursing practice' },
  { href: '/clinical-research/', label: 'Research' },
  { href: '/blog/', label: 'Articles' },
  { href: '/cv/', label: 'CV' }
];
---

<header class="site-header" aria-label="Primary navigation" data-site-header>
  <div class="container site-header__inner">
    <a class="brand" href="/" aria-label="Reeja Maharjan homepage">
      <span class="brand__mark" aria-hidden="true">RM</span>
      <span class="brand__text">
        <strong>{name}</strong>
        <small>{professionalFacts.nepalLicenseShort} / {professionalFacts.currentEmployerShort} Research Assistant</small>
      </span>
    </a>

    <nav class="desktop-nav" aria-label="Main navigation">
      {links.map((link, index) => (
        <a href={link.href} aria-current={link.href !== '/#experience' && current(link.href) ? 'page' : undefined}>
          <span aria-hidden="true">{String(index + 1).padStart(2, '0')}</span>
          {link.label}
        </a>
      ))}
      <a class="nav-cta" href="/hire-reeja/" aria-current={current('/hire-reeja/') ? 'page' : undefined} data-track="hire-nav">
        Contact
      </a>
    </nav>

    <button
      class="menu-toggle"
      type="button"
      aria-expanded="false"
      aria-controls="mobile-nav"
      aria-haspopup="dialog"
      aria-label="Menu"
    >
      <span class="menu-toggle__label">Index</span>
      <span class="menu-toggle__icon" aria-hidden="true"><i></i><i></i></span>
    </button>
  </div>

  <div
    class="mobile-nav"
    id="mobile-nav"
    role="dialog"
    aria-modal="true"
    aria-label="Mobile navigation"
    aria-hidden="true"
  >
    <nav class="mobile-nav__panel" aria-label="Mobile navigation links">
      <div class="mobile-nav__head">
        <div>
          <span class="mobile-nav__eyebrow">Contents</span>
          <strong>Professional field notes</strong>
        </div>
        <button class="mobile-nav__close" type="button" data-mobile-nav-close aria-label="Close menu">
          <span aria-hidden="true"></span>
        </button>
      </div>
      <a href="/" aria-current={path === '/' ? 'page' : undefined}><span aria-hidden="true">00</span>Home</a>
      {links.map((link, index) => (
        <a href={link.href} aria-current={link.href !== '/#experience' && current(link.href) ? 'page' : undefined}>
          <span aria-hidden="true">{String(index + 1).padStart(2, '0')}</span>
          {link.label}
        </a>
      ))}
      <a class="mobile-nav__cta" href="/hire-reeja/" aria-current={current('/hire-reeja/') ? 'page' : undefined} data-track="hire-mobile-nav">
        Discuss an opportunity
      </a>
    </nav>
  </div>
</header>
